# IHADIS Data Comparison Tool - Environment Variables
# Copy this file to .env and fill in your values

# Flask Configuration
FLASK_ENV=production
SECRET_KEY=your-secret-key-here-change-in-production

# Database
DATABASE_URL=sqlite:///data/app.db

# File Upload
UPLOAD_FOLDER=uploads
MAX_UPLOAD_SIZE_MB=50

# Encryption key for API keys stored in database
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
API_KEY_ENCRYPTION_SECRET=your-encryption-key-here

# Server Configuration (for production)
HOST=0.0.0.0
PORT=8000

# Note: Google Sheets credentials are configured via the UI
# Go to Settings > Google Sheets tab to paste your service account JSON
